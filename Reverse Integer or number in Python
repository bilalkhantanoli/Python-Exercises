# Input a number from the user
x = int(input("Enter number: "))
rev = 0

# Loop to reverse the number
while x > 0:
    rev = (rev * 10) + (x % 10)  # Extracts the last digit and adds it to the reversed number
    x = x // 10  # Removes the last digit from the original number and '//' is used to get pure integer value not float number 

# Display the reversed number
print(rev)
